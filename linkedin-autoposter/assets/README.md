# assets

Put your brand logo here as **`logo.png`** (transparent background recommended).

Once `assets/logo.png` exists, the image generator embeds it at the top of every
quote card. If it's missing, the card falls back to a text wordmark (your `HANDLE`).

Set your brand colors by editing the palette constants at the top of `../media_gen.py`.
